<% include ../partials/header %>

    <section>
        <div class="container my-4">
            <h1 class="text-center my-2 mb-3">Edit comment in<%= campground.name %></h1>
            <div class="row d-flex justify-content-center align-items-center">
                <div class="col-sm-10 col-md-6 col-lg-5">
                    <form action="/campgrounds/<%= campground.id %>/comments/<%=comment.id%>/edit?_method=PUT" method="POST">
                        <div class="form-group">
                            <label for="Text">Text</label>
                            <input type="text" class="form-control" name="comment[text]" id="" placeholder="" value="<%=comment.text%>">
                        </div>
                        <button type="submit" class="btn btn-primary btn-block">Update Comment</button>
                    </form>
                    <a href="/campgrounds">Go Back</a>
                </div>
            </div>
        </div>
    </section>
       

<% include ../partials/footer %>
